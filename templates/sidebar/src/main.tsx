import { render } from "@gpuix/react";
import { App } from "./app";
render(<App />, {
  title: "GPUIX Kit starter",
  appName: "GPUIX Kit starter",
  width: 1100,
  height: 760,
  minWidth: 900,
  minHeight: 640,
});
