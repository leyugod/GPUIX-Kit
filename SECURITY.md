# 安全反馈

本库不提供账户、数据库或在线服务。适配接口的真实权限、输入可信度和持久化策略由使用方负责；UI 的 readOnly/disabled 不是服务端授权。

请勿在公开 Issue 中提交密钥、用户数据或可直接利用的敏感细节。GitHub 仓库启用 private vulnerability reporting 后，可通过 Security 页面私下报告；若该渠道尚未开启，请先提交不包含漏洞细节的联系请求，请维护者提供私有沟通渠道。

建议提供受影响版本、最小复现、宿主平台和固定依赖版本。维护者尚未承诺响应时限或长期支持周期。
